{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx tsc src/main.ts --outDir src --module commonjs --target es2018 --esModuleInterop --skipLibCheck --moduleResolution node"
  },
  "deploy": {
    "startCommand": "node src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
